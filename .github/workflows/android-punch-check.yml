name: Native binding checks

on:
  pull_request:
    paths:
      - 'android/punchbridge/**'
      - 'android/build-libbox-release.sh'
      - 'ios/build-libbox-release.sh'
      - 'SINGBOX_VERSION'
      - '.github/workflows/android-punch-check.yml'
  push:
    branches: [main]
    paths:
      - 'android/punchbridge/**'
      - 'android/build-libbox-release.sh'
      - 'ios/build-libbox-release.sh'
      - 'SINGBOX_VERSION'
      - '.github/workflows/android-punch-check.yml'

jobs:
  go-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          cache-dependency-path: android/punchbridge/go.sum
      - name: Test OpenRung protocols and gomobile bindings
        working-directory: android/punchbridge
        run: go test -race ./...
      - name: Check native release build scripts
        run: bash -n android/build-libbox-release.sh ios/build-libbox-release.sh
